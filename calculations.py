def deposit(currentBalance, amount):
    currentBalance1 = amount + currentBalance
    return currentBalance1

def balance(currentBalance, amount):
    currentBalance2 =  amount - currentBalance
    return currentBalance2
